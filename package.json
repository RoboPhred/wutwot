{
  "name": "root",
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap",
    "clean": "lerna run --parallel clean",
    "build": "lerna run --scope homectrl-moziot build && lerna run --scope homectrl-plugin-test build && lerna run --scope homectrl-server build",
    "start": "lerna run --scope homectrl-server --stream start",
    "watch": "lerna run watch",
    "lerna": "lerna"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/RoboPhred/homectrl-server.git"
  },
  "keywords": [
    "mozilla-iot",
    "iot"
  ],
  "author": "William Matthews",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/RoboPhred/homectrl-server/issues"
  },
  "homepage": "https://github.com/RoboPhred/homectrl-server#readme",
  "devDependencies": {
    "lerna": "^3.4.1"
  }
}
